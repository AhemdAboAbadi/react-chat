{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm i && cd client && npm i && cd ..",
    "heroku-postbuild": "cd client && npm run build",
    "start": "npm start && cd client && npm i",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./server/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.15",
    "react-scroll-to-bottom": "^4.2.0",
    "socket.io": "^4.4.0"
  }
}
